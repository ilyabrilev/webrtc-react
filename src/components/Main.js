import ChatSection from "./ChatSection";
import VideoSection from "./VideoSection";

const Main = () => {
    return (
        <main className="main">
          <VideoSection />
          <ChatSection />
        </main>
    );
};

export default Main;